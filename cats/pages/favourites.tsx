import Layout from "@components/Layout";
import { NextPage } from "next";
import React from "react";

const favourites: NextPage = () => {
	return <Layout title="Favourites"></Layout>;
};

export default favourites;
